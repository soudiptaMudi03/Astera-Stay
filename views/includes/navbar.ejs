<style>
    .search_box{
        position: relative;
        left:-4rem;
    }
    .search_btn{
        display:flex;
        border-radius:20px;
    }
    .search_form{
        border-radius:20px;
        text-align: center;
    }
    .search_icon{
        margin-top:5px;
    }
</style>
<nav class="navbar navbar-expand-md bg-body-tertiary sticky-top border-bottom">
    <div class="container-fluid">
        <a class="navbar-brand" href="/home"><i class="fa-regular fa-compass"></i></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link" href="/home">Home</a>
                <a class="nav-link" href="/listings">Explore</a>
                <a class="nav-link" href="/listings/new">Add Place</a>
            </div>
            <div class="navbar-nav ms-auto">
                <div class="container-fluid ms-auto search_box">
                    <form class="d-flex" role="search">
                      <input class="form-control me-2 search_form" type="search" placeholder="Search Destinations" aria-label="Search">
                      <button class="btn btn-outline-success search_btn" type="submit"><i class="fa-solid fa-magnifying-glass search_icon"></i>&nbspSearch </button>
                    </form>
                </div>
            </div>
            <div class="navbar-nav ms-auto">
                <% if(!currUser) {%>
                <a class="nav-link" href="/signup">Signup</a>
                <a class="nav-link" href="/login">Login</a> 
                <% } %>
                <% if(currUser) {%>
                <a class="nav-link" href="/logout">Logout</a>
                <% } %>
            </div>
        </div>
    </div>
</nav>